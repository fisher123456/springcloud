spring:
  application:
    name: user
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
server:
  port: 8201
ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000
